# Environment variables for Docker Compose
# Copy this file to .env and customize the values

# Required: Path to your documents directory
# This directory will be mounted into the container for AI processing
DOCUMENTS_PATH=/path/to/your/documents

# Optional: Override default models
# OPENAI_API_MODEL_NAME=llama3.2
# OPENAI_API_EMBEDDING_MODEL_NAME=nomic-embed-text

# Optional: Override Ollama URL if running externally
# OPENAI_API_BASE_URL=http://localhost:11434/v1